# Echo — project context

Echo is a local-first "read YouTube instead of watching it" tool: **paste a YouTube link → get the transcript → AI digest → optionally save to a library**. Repo name is `echo` (the working dir is `yt_transcript`).

> **Continuity note:** detailed accumulated project memory is kept in a **private local store outside this repo** (not published). This file, the code, and git history are the public source of truth; when they disagree, trust the code + git history.

## Stack & how to run
- **Runtime:** Node **>=22.5** (uses the built-in `node:sqlite` — no native DB deps). ESM (`"type":"module"`).
- **Server:** `server.js` (Express, all routes). Binds `127.0.0.1:8000` by default; set `ECHO_HOST=0.0.0.0` to expose.
- **Frontend:** `public/index.html` — a single ~10k-line inline-everything monolith (HTML+CSS+JS, no build step). **The server caches index.html at boot** — restart the server to see frontend edits in dev.
- **AI:** goes through a provider seam (`providers.js`). Default = `ClaudeCliProvider` (shells the local `claude -p` CLI, keyless). `ApiKeyProvider` (Anthropic SDK) is used only when a per-request key is supplied (BYOK) or `ECHO_PROVIDER=api`.
- **Run:** `npm start` (local mode) · `npm test` (`node --test`) · `ECHO_MODE=web PORT=8080 node server.js` (hosted) · `ECHO_MODE=desktop node server.js`.

## The three modes (`ECHO_MODE`)
- **local** (default): local `claude` CLI, full server-side SQLite library, no limits. The dev/personal experience — **never break this** (hard constraint).
- **desktop**: like local + **optional BYOK** (a user without the CLI can add their own Anthropic key in Settings). Used by the Tauri shell.
- **web**: **BYOK required** (key per-request via `X-Echo-Api-Key`), library is client-side IndexedDB (server library routes are `blockInWeb` 503), per-IP rate limits + payload caps. Stateless — no server secrets/volume needed.

## Current direction & status (as of 2026-07-08)
Direction: **ship to others** — a hosted web BYOK app + a free desktop app, from one codebase. Done across prior sessions + this session on `main`:
- Web BYOK shell (onboarding, key validation, legal footer, `Dockerfile`/`.env.example`).
- Desktop BYOK mode (`ECHO_MODE=desktop`).
- Web-mode cost-sink hardening (`/api/usage` + `/api/playlist` gated; yt-dlp timeout; ccusage dedup).
- Fixed a Windows `spawn('*.cmd',{shell:false})` EINVAL crash in `usage.js`.
- Security/correctness hardening pass: resolveHref XSS scheme filter + /api/languages input validation + parallelized map-reduce + prompt-input sanitization + playlistJob rejection guard. Tests 154/154 green.
- **Feature cut**: removed embeddings/semantic search (+ `@xenova/transformers`), clips, notes, favorites, and saved highlights (all 0-use). **Kept** FTS5 keyword search, digest/ask/enrich, tags, find-in-transcript, Discovery, export, and the map-reduce digest fallback.
- **Desktop installers build (Linux)**: fixed the Tauri bundle (stale `embeddings.js`/`clips.js` refs, missing `websearch.js`/`discovery.js`/`usagelog.js`, dev-`node_modules` musl leak) so `npm run tauri:build` produces AppImage/`.deb`/`.rpm`; `lib.rs` compiles (`cargo check`, Rust 1.95) and the bundled backend drives a real transcript+digest end-to-end. Added `tests/tauri-bundle.test.js` as a drift guard. Tests 157/157 green.
- **Roadmap features shipped (2026-07-07):** A1 (multi-paste queue + `startBatchDigest` + `/api/batch/digest`), C2 (auto-tagging via dedicated `/api/tags/suggest`), C1 (ask-across-library RAG + `buildLibraryFtsQuery()` FTS5 OR-of-quoted-terms), B1 (vault.js markdown sync + `/api/vault/sync`), A2 (channel following: one-click Follow everywhere — Discovery cards, loaded videos, saved entries with self-heal channelUrl backfill; Inbox is paginated full-catalog card-grid browser; local/desktop-only). B2 (TTS read-aloud) intentionally dropped by user request. All tests pass 218/218, security-scanned, runtime-verified on dev tree.
- **"Signal" UI revamp (2026-07-08, `main` @ c5f296e):** full reskin from the templated cream/green look to **Signal** — a dark-first "listening instrument" aesthetic (near-black `#0A0B0D` / cyan `#3DE0C8` dark; `#FBFBFC` / `#0FA98D` light; light + dark both first-class). Signature = a waveform resolving into text. **CSS-only, no JS/backend touched**: both `:root` token blocks remapped while keeping every token *name*, so all JS-wired ids/classes reskinned untouched; added `--surface-2/3`, `--signal(-2)`, `--radius-lg`, `--font-ui`, mono instrument readouts (JetBrains Mono for numeric/temporal data, cyan spent only on active states). Landed in 3 phases (`e154597` hero/tokens · `c9252ba` library/discovery/inbox cards · `c5f296e` research/settings/modals). Also fixed 2 latent bugs: orphaned "Mark all seen/Load more" on empty inbox, and a `[hidden]` override where class-set `display` beat UA `[hidden]{display:none}` (so `el.hidden=true` did nothing) — added a scoped `[hidden]` guard. Tests 218/218; independently visual-QA'd on Windows (this machine) across landing/library/discovery/inbox/settings in light + dark + mobile.
- **Brand logo + ambient background (2026-07-08, follow-up session):** (a) **New waveform logo propagated everywhere** — the Signal cyan/indigo 6-bar waveform mark replaced the old green "ripple" in `public/favicon.svg`, `public/echo-logo.svg`, `public/echo-logo-{dark,light}.png` (README banner), and the full `src-tauri/icons/*` set (regenerated via `tauri icon` from a 1024² source; dropped the auto-generated `android/`/`ios/` dirs — desktop-only project). Before this the new mark lived *only* in the inline header SVG; every standalone asset was stale (favicon was still green, desktop icons were an unrelated placeholder). (b) **"Signal Surface" ambient background** in `public/index.html` — fixes the flat-black emptiness with a signal glow (corners) + faint oscilloscope graticule + micro-grain, behind a now-transparent `body` (base fill moved to `html`). The **glow is minimal at rest / when done** and **comes alive while the AI is digesting**: `setTopIndicator('digesting'|'working')` toggles `body.ai-active`, which eases glow opacity `.16→1` via a 1.6s transition (smooth bloom-in AND fade-out) plus a 3.6s breathing pulse kept on `filter` — **not** opacity: a CSS animation on opacity overrides the transition and snaps the glow in/out (an early version had exactly that abruptness). Disabled under `prefers-reduced-motion`, hidden in print. Theme-aware via `color-mix` on `--signal`/`--signal-2`/`--ink`; light theme pulls grain back so it stays clean. CSS-only + 1 line of JS. Tests 218/218, runtime-verified (wiring asserted, opacity interpolation sampled, rest/active screenshotted light+dark).
- **Design-scale token extraction (2026-07-08, d2b3fe0 + follow-ups):** StyleSeed audit-only (84→~93/100): extracted 9-step type scale (`--text-3xs..--text-3xl` + `--text-hero`; 148 sites, 30→10 raw values) + 16-step spacing scale (`--space-1..--space-16`; 385 sites, 47→16 raw values); all replacements snap to nearest step with ≤0.8px drift (imperceptible). Also: emoji→SVG icons on enrich buttons (💡/🔎/💬 → stroke SVGs, `innerHTML`-safe via static enum), header button radius unification (`.header-save-btn` 999px→`var(--radius-sm)`), and tokenized badge shadow. Tests 218/218, security-scanned, visual-QA'd dark/light/mobile. Gotcha: ~28-line token block shifted downstream line numbers — grep by content, not line. See [[echo-design-scale-tokens]].
- **Digest AI-tell audit & plain-writing tweak (2026-07-08):** ran avoid-ai-writing detector (MIT, v3.14.0) over all 21 saved digests → already clean, 2.6/100 mean score. Systemic tells identified: hollow-intensifiers ("genuinely") and corporate jargon ("robust", "leverage", "actionable"); minimal prompt tweak added to `digest.js` `structureInstructions` — one new rule to "write plainly, avoid filler + jargon". A/B verification on 2 videos: tier1 jargon dropped, quality preserved, scores 5→4 and 5→2. Vendored eval tool under `tools/ai-tell/` (patterns.js + score-digests.mjs) + `npm run digest:aitell` for ongoing monitoring. Tests 218/218. See [[echo-digest-ai-tell-eval]].
- **Shareable digest pages + web-verified claims (2026-07-08, 249 tests):** new `sharepage.js` module renders standalone HTML shares (`GET /s/:id`, unguessable `crypto.randomBytes(8)` id), strict CSP, Signal-themed with waveform hero + editorial reading body + mono "signal readout" meta strip + "Key claims" verification ledger (colored status rails: supported/unsupported/mixed/unverifiable). `digest.js` `extractClaims()` + `verifyClaims()` with bounded concurrency (4), reusing `searchWeb()` DDG path + LLM verdict; server `POST /api/claims` (webLimit, rate-limited, BYOK); frontend "Verify claims" on-demand + "Share" button via `POST /api/share` + `DELETE /api/share/:id` (blockInWeb). Share feature **local/desktop-only** v1 (web stateless; persistence/deploy decision deferred). Also fixed inbox empty-state stickiness: converged all four follow-completion handlers onto shared `loadFollowedList()` + `loadInbox()` + added `knownFollowCount` cache at START of `loadInbox()`. Tests 249/249 green, security-scanned, visual-QA'd light+dark+mobile. **Follow-up fix:** the empty `#shareResult` bar stayed visible at rest because its `.share-result{display:flex}` class beat the UA `[hidden]{display:none}` rule — added `.share-result[hidden]` to the scoped `[hidden]` guard block (same gotcha class as the inbox empty-state fix). See [[echo-share-and-claims]].
- **Research feature UI reshape: ephemeral floating popover (2026-07-08, 261 tests):** redesigned the digest "Research" enrich system from a heavy persistent UI (toolbar + inline cards + right-side rail drawer with session-persisted `enrichLog`) to a lightweight ephemeral floating popover. Highlighting digest text now shows `#enrichToolbar` anchored above the cursor with buttons **Verify / Explain / Background / Ask**; clicking a button renders the result INSIDE the popover (replacing any prior result), and re-anchors as it grows (max-height 320px + scroll, max-width 360px); dismiss (click-outside / Esc / new selection) clears — NOTHING persists. Renamed "Fact-check" button to **Verify** (backend mode key `'factcheck'` unchanged; `/api/enrich` route untouched). Removed `#researchBtn` header button + `#researchCount` + entire `#researchRail` drawer + inline enrich cards + `enrichLog` state/session persistence + 9 rail-related JS functions. Signature changes: `showEnrichToolbar(rect, text, context)` (dropped `endBlockEl` param); `runEnrichLookup(mode, selection, context, rect)` (was `...card, body`); `enrichButtonClick` rewritten to render in-popover + keep toolbar open. Tests 261/261 green, integration sweep clean (no dangling refs to removed symbols; changed signatures match all call sites). **Spot-check (2026-07-09):** the previously-advised manual check is done — verified end-to-end in real headless Chrome (Chrome 150) over CDP: the trigger (real mouseup+selection), all three dismiss paths (Esc / outside-click / scroll), and in-popover result rendering all pass (14/14). Caveat resolved; no code changes needed. See [[echo-research-popover]].
- **Public-launch feature audit + implementation (2026-07-10, `/grill-me` grilling) — DONE, landed on `main`:** scoped Echo for a public launch, then implemented the cuts across 7 commits (`e4bf01b`→`bfbc722`), 247/247 tests, each wave runtime-verified. **Root:** launch = a stranger's first _single-video web read_ (BYOK, stateless, free, no account); the differentiated digest is the only thing launch must prove. **Library** stays free browser-local (IndexedDB) at launch — accounts + a paid server-saved library are **Phase 2** (data shapes kept clean). Full audit + verdict table in [`PLAN.md`](PLAN.md). What shipped:
  - **Gated off in web (`blockInWeb`, code intact for local/desktop):** `/api/claims` (verify-claims), `/api/cross-digest`, `/api/playlist*`. Enrich **"Verify"** button hidden in web + `/api/enrich` rejects `mode:'factcheck'` in web (`FACTCHECK_DISABLED_IN_WEB`); the "Verify claims" button (`#digestVerifyBtn`) hidden in web too.
  - **Fully removed (unused):** the **Ask feature** — backend `/api/chat` + `askVideoQuestion`, AND the frontend Ask workspace sub-tab (`submitAsk`/`#askBtn`/`askThread`/session-restore) + the enrich popover's `#enrichBtnAsk` button (which only seeded that tab) + the now-dead answer-language machinery. (The backend was removed first; a follow-up P0 fix removed the frontend after a `vishnu-sentinel-scan` caught the leftover `/api/chat` caller — an initial grep wrongly reported "no frontend caller" because the call goes through an `aiFetch()` wrapper. The AI Workspace sub-nav was then removed entirely — only the digest remained, so the Digest lens now generates directly with no sub-tab.) **ask-across-library (C1)** — `POST /api/library/ask`, `askLibrary()`, `buildLibraryFtsQuery()`+stopwords (had no other caller; `/api/search` uses `searchLibrary()` directly, untouched), the "Ask your library" UI + its 2 tests; **batch/multi-paste (A1)** — `POST /api/batch/digest`, `startBatchDigest()` (playlist exports in `playlistJob.js` kept), the multi-paste overlay UI + its test.
  - **Dropped:** ccusage cost-display — deleted `usage.js`, `GET /api/usage`, the header `#usageChip`, and its tauri bundle-resource entry; **kept** `usagelog.js` (JSONL action meter) + `usage_stats.mjs`. Per-digest usage stats (`renderUsageStats`) untouched.
  - **Redesigned:** auto-tagging folded into the digest pass — `/api/digest` runs `generateDigest` + `suggestTags` concurrently (`Promise.all`, tag call best-effort with a 15s timeout that can never reject → digest response unchanged on tag failure), returns `suggestedTags: string[]`; frontend stashes them and applies at save via `patchSavedTags`; separate `/api/tags/suggest` route removed. Verified: real CLI digest returned relevant tags in 12.2s, no added latency, digest quality intact.
  - **Phase-2 backlog (deferred, not built):** ①web share (`/s/:id` via a minimal TTL'd store — first add after launch, the growth loop); ②accounts + server-saved library; ③Discovery+Follows/Inbox → YouTube-OAuth reading front-end (subscriptions-based feed is the shippable form — the true "For You" feed is NOT in YouTube's official API; real home feed = personal desktop-only scrape).

- **Frame-augmented digest (2026-07-13, `FRAMES.md`) — P1+P2 SHIPPED:** opt-in "Include visuals" toggle on the digest feeds **still frames** from the video into the digest so it can read on-screen content (charts, prices, slides) the transcript can't. `frames.js` (yt-dlp ≤480p → ffmpeg scene-detect + `mpdecimate` → **content-aware selection** by JPEG-size detail proxy → cap 24 → contiguous renumber); multimodal via `claude -p --allowedTools Read --add-dir` (keyless local, proven) or `ApiKeyProvider` base64 blocks (BYOK); `/api/digest` `includeVisuals` best-effort (degrades to text, `finally` cleanup, web-gated, videoId-guarded, returns `visualFrames`). **Local/desktop only — never web.** Grounded in a 3-video A/B experiment: value scales with how much a video SHOWS vs SAYS; the clamp is calibrated **numbers-encouraged / names-strict** (every fabrication in testing was a NAME, every win a NUMBER). Verified E2E: ~2× the concrete-figure density of the text-only digest, name-fabrication eliminated. 264 tests. **Needs ffmpeg on PATH** (or `ECHO_FFMPEG`). **P3 CLOSED — ship as-is:** all three improvement directions were experiment-gated and none delivered a clear win (see [`P3.md`](P3.md)) — (A) salience-weighted selection ≈ detail-only (4/24 frames differ, digests indistinguishable); (B) per-frame transcript context is *counterproductive* (makes the model paraphrase narration instead of reading frames); (density) scene-vs-dense candidate pool is marginal/within-noise. The shipped bag-of-frames + numbers-strict clamp is the sweet spot. See [[echo-frame-augmented-digest-test]].

- **Client-side folder-picker vault sync + web Obsidian deep-link (2026-07-13) — SHIPPED:** a **File System Access API** "Sync to vault folder…" flow that writes the whole library into an Obsidian vault folder the user picks. **100% client-side, no new server route** (the server-side `blockInWeb` `POST /api/vault/sync` stays as-is) — all in `public/index.html`, reusing `entryToMarkdownClient` + `Library.exportAll()`. It is **mode-agnostic and shown in ALL modes** (`#vaultFolderSync` block, gated only on `'showDirectoryPicker' in window`): in web it reads IndexedDB, in local/desktop it reads the server library — the sync functions don't care. `pickAndSyncVaultFolder`/`runVaultFolderSync`: `showDirectoryPicker({mode:'readwrite'})` → `FileSystemDirectoryHandle` persisted in a **dedicated `echo-vault-fs` IndexedDB** (localStorage can't hold handles), re-granted via `queryPermission`/`requestPermission`; writes one `slug-videoId.md` per entry with idempotent content-diffing → `{written,unchanged,failed}` (same filename + contract as desktop `vault.js`); **picking a fresh empty folder is how you create a new vault**. Fallbacks kept per mode: local/desktop keeps the typed-path `#vaultSyncBtn` (→ server `/api/vault/sync`); web keeps the ZIP export (folder button hides on non-Chromium → Firefox/Safari). Also: per-note **"Send to Obsidian"** (`.saved-obsidian-btn`, web-only) → `obsidian://new` deep-link, **digest-only** body with an 8000-char URI guard, optional vault name (`#obsidianVaultNameInput`, localStorage `echo-obsidian-vault`; blank = current vault). 264 tests, integration-scanned, runtime-verified in **both** web and local mode (console/CSP clean, correct block visibility per mode, URI+guard correct, light+dark screenshots). **Gotcha (real support incident):** the idempotent re-sync reports `0 written, N unchanged` when the vault is already current — this is success, not failure; the status line was reworded ("Vault already up to date — N files, nothing changed") so it doesn't read as broken. Native picker + real disk write is the one manual-only boundary.

- **Vault navigability: dashboard index note + summary frontmatter (2026-07-13) — SHIPPED:** fixed "the synced vault is a flat wall of 26 long filenames." Two additions, mirrored on BOTH sync paths — server (`markdown.js`/`vault.js` → typed-path `/api/vault/sync`) and the client folder-picker (`entryToMarkdownClient`/`runVaultFolderSync` in `public/index.html`): (a) **`extractSummary(digest)`** pulls each digest's `## TL;DR` (or first non-heading paragraph, ~240-char cap) and emits it as a `summary:` YAML frontmatter line on every note (shows in Obsidian's Properties view); (b) **`buildVaultIndex(items)`** writes an **`Echo Library.md`** dashboard MOC at the vault root with three views — **By date** (UTC month buckets, newest-first, each a `[[slug-videoId|Clean Title]]` wikilink + its summary line), **By topic**, **All notes** (A–Z). Pure/deterministic (no wall-clock in the body) so re-sync stays idempotent. **By topic only lists tag clusters of ≥2 videos** — this user's tags are almost all singletons (5 unique per video; only `Indonesia` clusters, ×4), so singletons would just recreate the wall; they stay reachable via Obsidian's built-in tag pane. Filenames kept as `slug-videoId.md` (renaming would orphan the 26 existing files into duplicates on next sync); the wikilink alias hides the ugly id. **Follow-up:** the index note alone didn't fix the Obsidian **file-explorer sidebar** (still a flat wall of 26 filenames), so notes are now filed into **`YYYY-MM/` month subfolders** by save date (`monthFolder(savedAt)` in `vault.js` + `monthFolderClient` in `index.html`; `Undated` for bad dates) — sidebar root becomes just `Echo Library.md` + collapsible month folders. Dashboard wikilinks are filename-only and resolve across folders (unique videoId), so `buildVaultIndex` was untouched; the index note stays at the vault root. Real vault migrated (deleted flat root `*.md`, re-synced → all 26 in `2026-07/`); idempotent (`0 written, 26 unchanged`). 273 tests, page-load clean. The index note is written but **not** counted in `syncVault`'s per-entry `written`/`unchanged`/`failed`; `syncVault` returns an added `index: 'written'|'unchanged'|'skipped'`. Server↔client markdown still differ in BODY only (transcript reflow) — frontmatter + index were unified. Real vault at `C:\Users\s.sani\Documents\Echo` regenerated + verified (26 notes + dashboard); 272 tests; sentinel-scan clean; page-load clean under CSP. See [[echo-vault-index-note]].

**What's left (all external-dependency-blocked):**
- Deploy the web app → `fly deploy` (see [`DEPLOY.md`](DEPLOY.md); Fly builds the image remotely, no local Docker needed).
- Confirm a real BYOK digest on the live site (needs a real Anthropic key; the CLI path is already proven end-to-end).
- Ship desktop installers → **Linux done** (`npm run tauri:build` → AppImage/`.deb`/`.rpm`, verified 2026-07-06 on Arch). Remaining: macOS/Windows installers (need those toolchains) + code-signing/notarization for distribution.

## Gotchas worth knowing
- **Removing a route? Grep the frontend for indirect callers, not just the literal path.** When cutting `/api/chat`, an initial scout reported "no frontend caller" and the route was deleted — but the Ask tab called it via an `aiFetch('/api/chat', …)` wrapper, leaving a 404-on-click P0. A `vishnu-sentinel-scan` after the fact caught it. Always: after removing an endpoint, integration-scan for the endpoint string AND for wrapper helpers (`aiFetch`/`apiFetch`) before calling it done.
- **Digest CLI isolation:** the `claude -p` digest subprocess must spawn with `cwd=tmpdir()` + an isolating `--system-prompt`, or it leaks this repo's own CLAUDE.md/memory into digests.
- **Windows `.cmd` spawns:** `spawn('*.cmd', args, {shell:false})` throws `EINVAL` synchronously on Windows (Node post-CVE-2024-27980). Use `shell:true` with a constant command string. yt-dlp is currently a real `.exe` here (unaffected); `claude` already routes via `cmd.exe /c`.
- **URL scheme filtering by host is unsafe:** `new URL('javascript:alert(1)').host === ''` (empty string), so host-based filters like `host.includes('duckduckgo.com')` pass `javascript:`/`file:`/`data:` URLs through. Always validate scheme with `/^https?:\/\//i` before using a URL. See `websearch.js` `resolveHref()`.
- **Tests miss runtime:** `node --test` doesn't spawn the real CLI or load the page under CSP. Before calling UI work done, drive a real digest + load the page (screenshots across light/dark/mobile).
- **Natural-language → FTS5 query:** raw questions passed to SQLite MATCH operator AND all tokens, so questions with stopwords return zero rows — if a future feature needs NL-question retrieval over the FTS5 store, strip stopwords and OR-join quoted tokens (e.g., "what is machine learning" → `("machine" OR "learning")` in MATCH syntax) rather than passing the raw question. (Previously lived in `store.js` as `buildLibraryFtsQuery()`, removed with the C1 ask-across-library feature cut.)
- **Test video:** use `youtube.com/watch?v=GRzaq5AHiV8` for manual/E2E checks.
- **Tauri bundle drift:** `tauri.conf.json` `bundle.resources` lists backend `.js` files individually — a new backend module (or a new `./x.js` import) that isn't added there crashes the desktop sidecar at runtime with `ERR_MODULE_NOT_FOUND`, and `node --test` / the `.deb`/`.rpm` bundlers never catch it (they don't start the backend). `tests/tauri-bundle.test.js` guards this. AppImage bundling also needs a production-only `node_modules` + `NO_STRIP=1` + `APPIMAGE_EXTRACT_AND_RUN=1`, all handled by `npm run tauri:build` (see [`DESKTOP.md`](DESKTOP.md)).
- **Old saved entries predate channel metadata:** Entries saved before A2 shipped lack `channelUrl`. Opening one triggers `GET /api/video-meta` (oEmbed, keyless, non-blocking) to back-fill the channel, revealing the one-click Follow button. Best-effort with navigation-race guard. Channel listings cannot pre-filter by transcript availability (YouTube doesn't expose it), so the Inbox card-grid shows all uploads; videos without transcripts fall back to the existing "no transcript" message on open.
- **Vendoring CommonJS into ESM:** importing a bare CommonJS third-party file (e.g., `module.exports`) into this ESM repo (`"type":"module"`) silently yields an empty namespace — `analyzeText()` becomes `undefined` with no error. Fix: create a scoped `package.json` in the vendor dir with `{"type":"commonjs"}` so files load as CJS, then import via `createRequire`. Don't touch the root `package.json` type. See `tools/ai-tell/` and [[echo-digest-ai-tell-eval]].
- **Self-contained CSP pages cannot load external fonts:** referencing web-font family names (e.g. 'Space Grotesk', 'Inter', 'JetBrains Mono') without an inline `@font-face` silently falls back to system fonts and looks generic. Use deliberate system font stacks (e.g. `-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`). See `sharepage.js` and [[echo-share-and-claims]].
- **File System Access API is Chromium-only + gated:** `window.showDirectoryPicker()` (used by web vault folder-sync) exists only in Chromium (Chrome/Edge, **not** Firefox/Safari), needs a **secure context** (https or localhost) and a **user gesture** (call it from a click handler — `requestPermission` also needs the gesture, so chain it off the same click). `FileSystemDirectoryHandle`s are **not** serializable to localStorage — persist them in **IndexedDB**. Always feature-detect (`'showDirectoryPicker' in window`) and fall back (Echo falls back to the ZIP export). Headless browsers usually lack the API, so a real E2E folder-write can't be automated — verify everything up to the native picker.
- **`obsidian://new` URI length ceiling:** deep-linking a note into Obsidian via `window.location.href = 'obsidian://new?…&content=…'` fails silently past the OS protocol-handler limit (well under a full transcript on Windows). Send **digest-only** markdown and guard the final URI length (Echo declines >8000 chars, pointing the user to folder-sync/ZIP). Custom-scheme navigation is **not** blocked by this app's CSP — `default-src` doesn't govern navigation and there's no `navigate-to`/`form-action` directive.
- **CSS comments in server-rendered HTML leak to output:** a literal `/* Key claims ... */` comment in a template string gets emitted into the HTML output. Tripped a test like `!output.includes('Key claims')`. Keep comment text generic or use variable names when rendering HTML server-side.

## Key docs
- [`DEPLOY.md`](DEPLOY.md) — Fly.io runbook (+ Railway note).
- [`DESKTOP.md`](DESKTOP.md) — Tauri desktop shell (Linux installers build; macOS/Windows + signing pending).
- [`PLAN.md`](PLAN.md) — feature-cut plan (now completed).
- [`FRAMES.md`](FRAMES.md) — frame-augmented digest (P1+P2 **shipped**): feed on-screen frames into the digest.
- [`P3.md`](P3.md) — proposed next step for frames: transcript↔frame alignment (anchored-slide selection; research-grade, gated on an experiment).
- [`WHISPER.md`](WHISPER.md) — proposed Whisper transcription fallback (not built; fixes bad/missing captions).
